/** Site identity & shared route constants used across layouts and pages. */
export const appName = 'Vault';

/** Base URL where the docs source is mounted. */
export const docsRoute = '/';

/** Route prefix for the OG card image endpoint. */
export const docsImageRoute = '/og';

/** Route prefix for the llms.mdx Markdown export endpoint. */
export const docsContentRoute = '/llms.mdx';

/** GitHub repo metadata used for "Edit on GitHub" / "View source" links. */
export const gitConfig = {
  user: 'thisonedev',
  repo: 'thisonedev.github.io',
  branch: 'master',
};
